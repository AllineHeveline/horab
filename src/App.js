import './App.scss';
// Importa o arquivo de estilos `App.scss`, que pode conter regras CSS/SCSS personalizadas para estilizar este componente.
// O uso de SCSS (Sass) permite uma escrita mais avançada de estilos com variáveis, aninhamento e outras funcionalidades do Sass.

function App() {
  // Define o componente funcional `App`, que é o componente principal da aplicação.
  
  return (
    <>
      {/* As tags vazias `<> </>` representam o Fragment do React, que permite agrupar múltiplos elementos JSX sem adicionar um nó extra ao DOM */}
    </>
  );
}

export default App;
// Exporta o componente `App` para que ele possa ser importado e usado em outras partes da aplicação.